# 安全设计与威胁建模

> 最后整理: 2026-05-22  
> 当前状态: `legacy-live`

本文档是安全治理的历史详细设计稿，适合回看威胁模型、认证授权、数据保护与供应链安全在早期如何被系统化组织；它不等同于当前安全实现与规范的唯一 owner。

## Page Role

- 这里负责：安全目标、STRIDE 威胁建模、认证授权、数据保护与安全测试的历史方案。
- 不负责：当前代码中的安全实现事实、现行中间件顺序、当前运维审计配置结论。

## Recommended Read Path

1. [README.md](./README.md)
2. [../../architecture/README.md](../../architecture/README.md)
3. [../../standards/README.md](../../standards/README.md)
4. [../../implementation/README.md](../../implementation/README.md)

## Boundary

- 如果你要找“当前安全边界 owner”，优先看 [../../architecture/README.md](../../architecture/README.md)。
- 如果你要找“现行规范口径”，优先看 [../../standards/README.md](../../standards/README.md)。
- 如果你要找“已落地的实施记录”，优先看 [../../implementation/README.md](../../implementation/README.md)。
- 本页更适合解释历史安全设计主线，不适合作为当前实现事实的唯一依据。

## Quick Section Map

| 如果你想看 | 直接跳到 |
|------|------|
| 安全目标和范围 | [1. 目标与范围](#1-目标与范围) |
| 威胁建模方法和攻击面 | [2. 威胁建模方法](#2-威胁建模方法) |
| 认证、会话和访问控制 | [3. 身份认证与会话安全](#3-身份认证与会话安全) / [4. 访问控制](#4-访问控制) |
| 输入校验、抗刷与数据安全 | [5. 输入校验与抗刷](#5-输入校验与抗刷) / [6. 数据与日志安全](#6-数据与日志安全) |
| 密钥、供应链、审计与安全测试 | [7. 配置与密钥管理](#7-配置与密钥管理) / [8. 供应链与依赖安全](#8-供应链与依赖安全) / [9. 可观测与审计](#9-可观测与审计) / [10. 安全测试](#10-安全测试) |

## Quick Takeaways

- 这篇最值得先看的，是它把威胁模型、访问控制、数据保护和供应链安全放在同一条安全主线上。
- 如果你只关心系统安全边界，通常先看“2. 威胁建模方法”“3. 身份认证与会话安全”“4. 访问控制”“6. 数据与日志安全”就够了。
- 末尾关联文档名单偏历史串联，不能直接替代当前 owner 导航。

## Skip Guide

- 只想知道“威胁模型怎么做”：看 [2. 威胁建模方法](#2-威胁建模方法)。
- 只想知道“认证和授权怎么考虑”：看 [3. 身份认证与会话安全](#3-身份认证与会话安全) 和 [4. 访问控制](#4-访问控制)。
- 只想知道“日志、密钥和审计怎么管”：看 [6. 数据与日志安全](#6-数据与日志安全)、[7. 配置与密钥管理](#7-配置与密钥管理)、[9. 可观测与审计](#9-可观测与审计)。
- 如果你当前只关心现网事实或现行规范，不必细读本页；优先回到 `docs/architecture/`、`docs/standards/`、`docs/implementation/`。

定义系统在身份认证、访问控制、输入校验、数据保护、运维安全与供应链安全等方面的策略，并以威胁模型指导落地与验证。

## 1. 目标与范围

- 保护用户与项目数据的机密性、完整性与可用性
- 降低被恶意滥用、越权访问、数据泄露与供应链风险
- 覆盖 API、服务端、数据库与外部 AI 提供商调用

## 2. 威胁建模方法

- 方法：STRIDE（伪造、篡改、抵赖、信息泄露、拒绝服务、权限提升）
- 资产：账号凭据、JWT 秘钥、用户/项目/文档数据、AI 调用配额与密钥
- 信任边界：客户端 ↔ 网关/路由 ↔ API ↔ Service ↔ DB/外部服务
- 攻击面：认证与会话、输入参数、批量接口、AI 外部调用、日志与备份

## 3. 身份认证与会话安全

- JWT：HS/RS 签名；短期 Access Token + 刷新机制；秘钥轮换与吊销策略
- 传输：Authorization: `Bearer <token>`；HTTPS 强制；服务端不落盘敏感 token
- 刷新与吊销：刷新令牌一次性/轮换；服务端维护黑名单/版本号（可选）
- 最小化声明：JWT 仅包含必要声明（sub/user_id、exp、iat、scope）

## 4. 访问控制

- 授权模型：RBAC/项目级 ACL（owner、member、viewer 预留）
- 资源校验：以 project_id/node_id 为边界做服务层授权校验
- 多租户隔离：按 owner_id/项目维度建立查询与索引，避免越权枚举

## 5. 输入校验与抗刷

- 输入验证：类型/范围/长度/格式；拒绝危险运算符（如 MongoDB 注入）
- 速率限制：IP 与用户双维度限流；认证接口叠加验证码/失败退避
- 幂等与重放：对写操作使用幂等键/版本号；时间戳+签名（可选）

## 6. 数据与日志安全

- 数据加密：敏感字段加盐哈希/加密存储（如密码、密钥）；最小字段暴露
- 日志脱敏：对 email/token/phone 等做掩码；错误日志不含敏感明文
- 备份安全：备份介质加密，访问审计与轮换；恢复演练

## 7. 配置与密钥管理

- 秘钥来源：环境变量/密钥管理服务（不进入代码仓库）
- 轮换策略：JWT 秘钥、AI Provider Key 定期轮换；灰度发布与回滚
- 最小权限：数据库账号按最小权限授权，区分读写/只读（如有）

## 8. 供应链与依赖安全

- 依赖审计：启用 SCA/漏洞扫描；最小化依赖；及时升级补丁
- 构建可信：锁定 go.mod/go.sum；CI 校验依赖签名与哈希

## 9. 可观测与审计

- 审计日志：登录、权限变更、敏感操作留痕并聚合存储
- 监控与告警：结合《日志与监控》中的指标阈值，异常行为告警

## 10. 安全测试

- 覆盖：鉴权/越权、注入、暴力破解、敏感信息泄露、限流绕过
- 方法：单测+集成测试+扫描器（SAST/DAST）+模糊测试

## 11. 关联文档

- 软件需求规格说明书(SRS) ./软件需求规格说明书(SRS).md
- 架构设计说明书 ./架构设计说明书.md
- 数据库设计说明书 ./数据库设计说明书.md
- API 接口总览 ./API接口总览.md
- 测试计划与用例 ./测试计划与用例.md
- 部署与运维指南 ./部署与运维指南.md
- 日志与监控 ./日志与监控.md
- 需求追踪矩阵 ./需求追踪矩阵.md
- JWT 身份认证设计 ./功能设计/JWT身份认证设计.md
- 管理后台设计 ./功能设计/管理后台设计.md
