# API Guide Mirror Notice

`Qingyu_backend/docs/guides/api` 当前不是 API 文档的 canonical owner。

## Current Source Of Truth

- 当前 API 文档主入口请看 [../../api/README.md](../../api/README.md)。
- Swagger 产物与导出说明统一留在 `Qingyu_backend/docs/api/`，不要继续写入本目录。

## Status Of This Directory

- 本目录是历史镜像区，保留旧引用和旧入口，避免立即大搬迁造成断链。
- 其中部分文件与 `docs/api/` 仍是同名镜像，另有部分内容已出现分叉。
- 在未逐篇比对前，不应直接删除本目录下的旧文件。

## Rule

1. 新增 API 参考、Swagger 说明、接口总览时，只写 `Qingyu_backend/docs/api/`。
2. 本目录只允许补迁移提示、索引修正或历史说明，不再继续扩展正文。
3. 若发现本目录某篇文档仍被外部引用，优先把引用切回 `docs/api/`，再考虑归档。
