# 第5章 代码截图附录

## 附录说明

本附录按第5章"系统实现"各小节顺序，列出需要截图的API层代码位置及对应的论文说明文字。所有代码截图均采用API层，代码说明描述API层的职责（参数绑定、调用Service、响应封装），便于插入论文正文。

---

## 5.0 数据层与多数据库实现

### 图索引 5.0.1 MongoDB主数据查询代码

**文件位置**：`Qingyu_backend/repository/bookstore/book_detail_repository.go`
**函数名**：`GetBookByID` / `ListBooks`
**对应章节**：5.0.1 MongoDB主数据实现

**代码说明**：该函数实现了基于MongoDB的主数据查询逻辑。首先通过`primitive.ObjectIDFromHex`将字符串格式的书籍ID转换为MongoDB对象ID，验证ID格式的有效性。随后构造查询条件过滤器`bson.M{"_id": bookID}`，通过`collection.FindOne`方法从数据库中获取单条书籍记录。若数据库中不存在该ID对应的记录，函数返回`ErrDocumentNotFound`错误；查询成功则通过`Decode`方法将BSON格式的文档解码为结构体实例返回。具体代码如图5.0.1所示。

---

### 图索引 5.0.2 Redis缓存读写代码

**文件位置**：`Qingyu_backend/pkg/cache/redis_client.go`
**函数名**：`Get` / `Set`
**对应章节**：5.0.2 Redis缓存与会话实现

**代码说明**：该Redis客户端封装实现了"读-写"两类缓存操作。`Get`方法通过`redis.Client.Get`从Redis中获取指定键的值，若键不存在则返回`redis.Nil`错误，调用方可据此判断缓存是否命中。`Set`方法支持设置键值对及过期时间（TTL），通过`client.Set`的`expiration`参数实现缓存自动失效，避免脏数据长期驻留。具体代码如图5.0.2所示。

---

## 5.1 读者端功能模块

### 图索引 5.1.1 用户登录验证代码

**文件位置**：`Qingyu_backend/api/v1/auth/auth_api.go`
**函数名**：`Login`（第77-91行）
**对应章节**：5.1.1 登录注册界面
**论文图号**：图 2

**代码说明**：登录API层实现了用户身份认证的接口入口。函数首先通过`c.ShouldBindJSON`绑定请求体到`LoginRequest`结构体，若JSON格式错误则返回400参数错误响应。随后调用`authService.Login`方法执行业务逻辑：Service层通过查询数据库验证用户名与密码的匹配关系，使用bcrypt对比密码哈希值。认证成功后，Service层生成JWT令牌并返回用户基本信息；若用户名不存在或密码错误，则返回401未授权错误。最后通过`response.SuccessWithMessage`封装统一格式的成功响应返回前端。具体代码如图2所示。

---

### 图索引 5.1.2 用户注册API层代码

**文件位置**：`Qingyu_backend/api/v1/auth/auth_api.go`
**函数名**：`Register`（第38-62行）
**对应章节**：5.1.1 登录注册界面
**论文图号**：图 4（原为Service层，改为API层）

**代码说明**：注册API层负责接收用户注册请求并调用Service层完成注册流程。函数首先通过`shared.ValidateRequest`方法验证请求参数的合法性，包括邮箱格式、密码长度等基础校验。若系统启用了邮箱验证功能，通过`codeManager.VerifyRegisterCode`方法校验用户提交的验证码是否正确，验证码错误或过期则返回400错误。参数校验通过后，调用`authService.Register`方法执行注册业务逻辑：Service层检查邮箱唯一性，进行密码哈希加密，创建用户记录并生成JWT令牌。最后通过`response.SuccessWithMessage`返回注册成功响应及用户令牌。具体代码如图4所示。

---

### 图索引 5.1.3 首页书籍列表获取代码

**文件位置**：`Qingyu_backend/api/v1/bookstore/bookstore_api.go`
**函数名**：`GetHomepage`（第64-72行）
**对应章节**：5.1.2 首页界面
**论文图号**：图 6

**代码说明**：首页API层实现了获取首页推荐书籍列表的接口。函数通过`api.service.GetHomepageData`方法调用Service层获取热门书籍、推荐书籍与最新更新书籍的合集。Service层通过聚合查询按浏览量、收藏量、更新时间等维度排序，返回推荐书籍列表。API层通过`response.SuccessWithMessage`方法封装统一响应格式返回前端，前端据此渲染首页分区卡片。该函数仅9行代码，职责单一，符合API层"参数绑定-调用Service-响应封装"的设计原则。具体代码如图6所示。

---

### 图索引 5.1.4 书籍筛选API层代码

**文件位置**：`Qingyu_backend/api/v1/bookstore/bookstore_api.go`
**函数名**：`SearchBooks`（第314-553行）
**对应章节**：5.1.3 书城浏览界面
**论文图号**：图 9

**代码说明**：书籍筛选API层实现了多条件组合筛选的接口入口。函数首先通过URL查询参数解析关键词、分类ID、作者、状态、标签等筛选条件以及分页参数。调用`bookstoreService.SearchBooksWithFilter`方法执行筛选逻辑：Service层构造查询条件，执行数据库查询并返回符合筛选条件的书籍列表。API层通过`response.SuccessWithMessage`方法封装响应，包含书籍数组、总记录数等信息返回前端。具体代码如图9所示。

---

### 图索引 5.1.5 书籍详情获取代码

**文件位置**：`Qingyu_backend/api/v1/bookstore/book_detail_api.go`
**函数名**：`GetBookDetail`（第37-55行）
**对应章节**：5.1.4 书籍详情界面
**论文图号**：图 11

**代码说明**：书籍详情API层实现了根据ID获取书籍信息的接口。函数首先通过`c.Param("id")`从URL路径中提取书籍ID字符串，若为空则返回400参数错误响应。随后调用`bookstoreService.GetBookByID`方法查询业务层：Service层从MongoDB数据库获取书籍完整信息。若书籍不存在或状态不可用（如草稿、已下架），Service层返回错误，API层转换为404未找到响应返回前端。查询成功则通过`response.Success`方法返回书籍详情JSON数据。该函数结构清晰、逻辑简洁，体现了单一职责原则。具体代码如图11所示。

---

### 图索引 5.1.6 章节内容获取代码

**文件位置**：`Qingyu_backend/api/v1/reader/chapter_api.go`
**函数名**：`GetChapterContent`（第52-85行）
**对应章节**：5.1.5 章节阅读界面
**论文图号**：图 14

**代码说明**：章节内容获取API层负责响应前端章节阅读请求。函数首先通过`shared.GetRequiredParam`解析URL路径参数中的书籍ID和章节ID，验证参数有效性。随后调用`chapterService.GetChapterContent`方法获取章节内容并校验阅读权限。该Service方法返回包含权限检查的章节内容响应对象，若用户无权访问（如VIP章节未付费），Service层返回相应错误。API层根据Service层返回的错误类型（章节不存在、未发布、访问拒绝）构造对应的HTTP响应，最后通过`response.Success`方法返回章节内容，前端渲染Markdown格式的正文供用户阅读。具体代码如图14所示。

---

### 图索引 5.1.7 阅读进度保存代码

**文件位置**：`Qingyu_backend/api/v1/reader/progress_api.go`
**函数名**：`SaveReadingProgress`（第79-98行）
**对应章节**：5.1.5 章节阅读界面
**论文图号**：图 15

**代码说明**：阅读进度保存API实现了用户阅读状态的持久化接口。函数通过`shared.BindAndValidate`绑定请求体到`SaveProgressRequest`结构体，包含`BookID`书籍ID、`ChapterID`章节ID、`Progress`阅读进度（0-1浮点数）等字段。获取当前用户ID后调用`readerService.SaveReadingProgress`方法更新数据库中的阅读记录：若该用户对该章节的阅读记录已存在则执行更新操作，否则创建新记录。Service层同时处理阅读时长统计，为个性化推荐提供数据支持。成功后通过`response.Success`返回200响应，前端据此更新本地阅读状态缓存。具体代码如图15所示。

---

### 图索引 5.1.8 章节阅读API层代码

**文件位置**：`Qingyu_backend/api/v1/reader/chapter_api.go`
**函数名**：`GetChapterContent`（第52-85行）
**对应章节**：5.1.5 章节阅读界面
**论文图号**：图 16

**代码说明**：章节阅读API层体现了"权限前置"的设计原则在接口层的体现。函数接收书籍ID和章节ID请求参数，调用Service层的`GetChapterContent`方法获取章节内容与权限信息。Service层执行元数据获取、发布状态检查、VIP/付费权限校验等业务逻辑。API层根据Service层返回的错误类型构造响应：章节不存在返回404，章节未发布返回403，访问拒绝也返回403但仍返回章节标题等元信息，前端据此展示"付费解锁"提示而非空页面。这种设计提升了用户体验，将权限校验结果转化为友好的界面反馈。具体代码如图16所示。

---

### 图索引 5.1.9 评论发表代码

**文件位置**：`Qingyu_backend/api/v1/social/comment_api.go`
**函数名**：`CreateComment`（第48-76行）
**对应章节**：5.1.6 评论发表界面
**论文图号**：图 18

**代码说明**：评论发表API层接收用户的评论与评分提交。函数首先通过`shared.BindAndValidate`绑定请求体到`CreateCommentRequest`结构体，包含`BookID`书籍ID、`ChapterID`章节ID（可选）、`Content`评论内容、`Rating`评分（0-5星）等字段。参数通过结构体标签进行验证：评分必须在0至5之间，内容长度在10-500字符之间。随后调用`commentService.PublishComment`方法持久化评论记录至数据库。创建成功后，Service层更新书籍的评分统计数据，确保评分数据的一致性。最后通过`response.Created`返回201创建成功响应。具体代码如图18所示。

---

### 图索引 5.1.10 添加收藏代码

**文件位置**：`Qingyu_backend/api/v1/social/collection_api.go`
**函数名**：`AddCollection`（第47-83行）
**对应章节**：5.1.7 收藏管理界面
**论文图号**：图 21

**代码说明**：添加收藏API层实现了用户收藏书籍的接口。函数通过`shared.BindAndValidate`绑定请求体到`AddCollectionRequest`结构体，包含`BookID`书籍ID、`FolderID`收藏夹ID（可选）、`Note`备注、`Tags`标签等字段。获取当前用户ID后调用`collectionService.AddToCollection`方法执行收藏逻辑：Service层判断用户是否已收藏该书籍，若已存在则返回错误，防止重复收藏。收藏成功后Service层同步更新书籍的收藏计数，保持统计数据一致性。API层根据错误类型返回相应响应：重复收藏返回400错误，成功返回201响应。具体代码如图21所示。

---

### 图索引 5.1.11 收藏列表查询代码

**文件位置**：`Qingyu_backend/api/v1/social/collection_api.go`
**函数名**：`GetCollections`（第209-233行）
**对应章节**：5.1.7 收藏管理界面
**论文图号**：图 22

**代码说明**：收藏列表查询API支持分页获取用户的收藏记录。函数获取当前用户ID后，从URL查询参数中解析`folder_id`收藏夹ID（可选），通过`shared.GetPaginationParamsStandard`获取分页参数。调用`collectionService.GetUserCollections`方法查询当前用户的收藏列表。Service层通过Repository执行数据库查询，按收藏时间倒序排列，支持按收藏夹ID筛选。返回结果包含书籍详情与收藏元数据（收藏时间、备注、标签），API层通过`shared.RespondWithPaginated`构造分页响应，前端据此渲染收藏管理页面。具体代码如图22所示。

---

### 图索引 5.1.12 个人信息获取代码

**文件位置**：`Qingyu_backend/api/v1/user/handler/profile_handler.go`
**函数名**：`GetProfile`（第48-78行）
**对应章节**：5.1.8 个人中心界面
**论文图号**：图 25

**代码说明**：个人信息获取API层实现了用户资料查询接口。函数从gin.Context中提取当前登录用户的ID（由JWT中间件注入），调用`userService.GetUser`方法获取用户完整资料。Service层查询数据库获取用户基本信息、阅读统计数据、会员状态等数据并聚合返回。API层根据Service层返回的错误类型构造响应：用户不存在返回404，其他错误返回500，成功则通过`shared.Success`封装用户资料对象返回前端，前端据此渲染个人中心页面。具体代码如图25所示。

---

## 5.2 作者端功能模块

### 图索引 5.2.1 创建作品API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/project_api.go`
**函数名**：`CreateProject`（第37-57行）
**对应章节**：5.2.1 作品管理界面
**论文图号**：图 29

**代码说明**：创建作品API层负责接收作者的项目创建请求。函数首先通过`c.ShouldBindJSON`绑定请求体到`CreateProjectRequest`结构体，若JSON格式错误或必填字段缺失则返回400参数错误响应。随后从gin.Context中提取当前登录用户的ID，通过`context.WithValue`将其注入请求上下文，确保Service层可获取操作者身份用于权限校验。调用`projectService.CreateProject`方法执行业务逻辑：验证项目名称唯一性、创建项目文档、初始化默认章节结构等。创建成功后返回201状态码及项目详细信息。具体代码如图29所示。

---

### 图索引 5.2.2 获取作品列表API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/project_api.go`
**函数名**：`ListProjects`（第99-125行）
**对应章节**：5.2.1 作品管理界面
**论文图号**：图 31

**代码说明**：获取作品列表API层支持分页与筛选查询作者的项目。函数从URL查询参数中解析`page`页码、`pageSize`每页数量、`status`状态筛选、`category`分类筛选等参数，构造`ListProjectsRequest`请求对象。同样从Context中提取用户ID并注入请求上下文。调用`projectService.ListMyProjects`方法查询当前用户的项目列表，Service层根据筛选条件构造MongoDB查询过滤器，执行分页查询后返回项目数组与总数。最后通过`response.Success`方法返回统一的分页响应格式。具体代码如图31所示。

---

### 图索引 5.2.3 获取作品详情API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/project_api.go`
**函数名**：`GetProject`（第69-85行）
**对应章节**：5.2.1 作品管理界面
**论文图号**：图 33

**代码说明**：获取作品详情API层实现了根据项目ID获取项目信息的接口。函数通过`c.Param("id")`从URL路径中提取项目ID，随后提取当前用户ID并注入上下文。调用`projectService.GetProject`方法查询项目详情，Service层会验证当前用户是否为项目所有者，非所有者访问将被拒绝。查询成功后API层返回项目详细信息，包含项目元信息、章节结构、统计数据等。具体代码如图33所示。

---

### 图索引 5.2.4 更新作品API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/project_api.go`
**函数名**：`UpdateProject`（第139-160行）
**对应章节**：5.2.1 作品管理界面
**论文图号**：图 35

**代码说明**：更新作品API层实现了修改项目信息的接口。函数提取URL路径中的项目ID，通过`c.ShouldBindJSON`绑定请求体到更新请求结构体。提取当前用户ID并注入context.Context后，调用`projectService.UpdateProject`方法执行更新逻辑。Service层验证用户是否为项目所有者，更新项目字段并保存至数据库。API层根据更新结果构造响应，成功返回200及更新后的项目信息。具体代码如图35所示。

---

### 图索引 5.2.5 删除作品API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/project_api.go`
**函数名**：`DeleteProject`（第172-187行）
**对应章节**：5.2.1 作品管理界面
**论文图号**：图 37

**代码说明**：删除作品API层实现了项目删除的接口。函数提取URL路径中的项目ID与当前用户ID，并将用户ID注入context.Context后，调用`projectService.DeleteProject`方法执行删除逻辑。Service层验证用户权限后执行软删除操作，将项目状态标记为已删除而非物理删除记录，保留历史数据便于审计。API层根据删除结果构造响应返回前端。具体代码如图37所示。

---

## 5.2.2 章节编写界面

### 图索引 5.2.6 创建章节API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/document_api.go`
**函数名**：`CreateDocument`（第47-93行）
**对应章节**：5.2.2 章节编写界面
**论文图号**：图 39

**代码说明**：创建章节API层实现了在项目中创建新章节的接口。函数首先验证文档服务是否初始化，然后从URL路径参数中提取项目ID。验证项目ID非空后，从gin.Context中获取当前登录用户的ID并转换为字符串格式。将用户ID注入context.Context后，通过`c.ShouldBindJSON`绑定请求体到`CreateDocumentRequest`结构体，设置项目ID并调用`documentService.CreateDocument`方法执行创建逻辑。Service层检查项目权限、创建章节记录、初始化章节状态。API层根据Service层返回结果构造响应：成功返回201及章节详情，失败则返回相应错误响应。具体代码如图39所示。

---

### 图索引 5.2.7 更新章节API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/document_api.go`
**函数名**：`UpdateDocument`（第151-168行）
**对应章节**：5.2.2 章节编写界面
**论文图号**：图 40

**代码说明**：更新章节API层实现了修改章节信息的接口。函数从URL路径参数中提取文档ID，通过`c.ShouldBindJSON`绑定请求体到`UpdateDocumentRequest`结构体。将当前用户ID注入context.Context后，调用`documentService.UpdateDocument`方法执行更新逻辑。Service层验证用户权限、更新章节字段、保存至数据库。API层根据Service层返回结果构造响应：权限不足返回403，章节不存在返回404，成功则返回200。这种设计确保章节更新操作的安全性，仅允许章节所有者或项目成员进行修改。具体代码如图40所示。

---

### 图索引 5.2.8 自动保存API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/editor_api.go`
**函数名**：`AutoSaveDocument`（第40-69行）
**对应章节**：5.2.2 章节编写界面
**论文图号**：图 41

**代码说明**：自动保存API层实现了编辑器自动保存功能的接口。函数从URL路径参数中提取文档ID，通过`c.ShouldBindJSON`绑定请求体到`AutoSaveRequest`结构体，包含文档ID、内容、版本号等字段。将当前用户ID注入context.Context后，调用`documentService.AutoSaveDocument`方法执行自动保存逻辑。Service层执行版本冲突检测：若版本号不匹配，返回"版本冲突"错误，API层捕获该错误并返回409 Conflict响应，提示用户刷新后重试。版本匹配则保存内容并更新版本号。这种设计避免了多端编辑时的数据覆盖问题，确保编辑体验的流畅性与数据一致性。具体代码如图41所示。

---

### 图索引 5.2.9 获取章节内容API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/editor_api.go`
**函数名**：`GetDocumentContent`（第107-123行）
**对应章节**：5.2.2 章节编写界面
**论文图号**：图 41a

**代码说明**：获取章节内容API层实现了编辑器加载章节内容的接口。函数从URL路径参数中提取文档ID，将当前用户ID注入context.Context后，调用`documentService.GetDocumentContent`方法获取章节完整内容。Service层查询数据库获取章节的标题、正文、元数据等信息，返回包含内容与版本号的内容对象。API层通过`response.Success`方法返回章节内容，前端编辑器据此加载章节内容供作者编辑。该接口是编辑器初始化的核心入口，确保作者打开编辑器时能快速加载章节内容。具体代码如图41a所示。

---

## 5.2.4 发布管理界面

### 图索引 5.2.9 发布作品API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/publish_api.go`
**函数名**：`PublishProject`（第36-65行）
**对应章节**：5.2.4 发布管理界面
**论文图号**：图 42

**代码说明**：发布作品API层实现了将项目发布到书城的接口。函数从URL路径参数中提取项目ID，验证非空后通过`c.ShouldBindJSON`绑定请求体到`PublishProjectRequest`结构体。从gin.Context中提取当前用户ID，调用`publishService.PublishProject`方法执行发布逻辑。Service层验证项目状态、检查发布权限、构造发布记录、同步至书城平台。API层根据Service层返回结果构造响应：成功返回200及发布记录，失败则返回相应错误响应。该接口支持多平台发布，通过请求参数指定目标书城，一次发布可同步至多个平台。具体代码如图42所示。

---

### 图索引 5.2.11 取消发布API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/publish_api.go`
**函数名**：`UnpublishProject`（第78-101行）
**对应章节**：5.2.4 发布管理界面
**论文图号**：图 43

**代码说明**：取消发布API层实现了将项目从书城下架的接口。函数从URL路径参数中提取项目ID，验证非空后从gin.Context中提取当前用户ID。调用`publishService.UnpublishProject`方法执行取消发布逻辑。Service层验证用户权限、更新发布状态、通知书城平台下架。API层根据Service层返回结果构造响应：权限不足返回403，项目不存在返回404，成功则返回200。取消发布后，作品将从书城隐藏，已发布的章节仍保留历史版本供已购买用户访问。具体代码如图43所示。

---

### 图索引 5.2.12 发布状态查询API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/publish_api.go`
**函数名**：`GetProjectPublicationStatus`（第113-128行）
**对应章节**：5.2.4 发布管理界面
**论文图号**：图 44

**代码说明**：发布状态查询API层实现了获取项目发布状态的接口。函数从URL路径参数中提取项目ID，验证非空后调用`publishService.GetProjectPublicationStatus`方法查询发布状态。Service层返回包含发布状态、发布时间、各平台发布情况、统计数据等信息的发布状态对象。API层通过`response.Success`方法返回发布状态数据，前端据此渲染发布管理页面，显示作品的发布情况与各平台的统计信息。该接口支持实时查询发布状态，作者可随时了解作品的发布进度与数据表现。具体代码如图44所示。

---

### 图索引 5.2.13 发布章节API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/publish_api.go`
**函数名**：`PublishDocument`（第143-179行）
**对应章节**：5.2.4 发布管理界面
**论文图号**：图 45

**代码说明**：发布章节API层实现了将单个章节发布到书城的接口。函数从URL路径参数中提取文档ID，验证非空后通过`c.ShouldBindJSON`绑定请求体到`PublishDocumentRequest`结构体。从gin.Context中提取当前用户ID，从查询参数获取项目ID（可选），调用`publishService.PublishDocument`方法执行章节发布逻辑。Service层验证章节状态、构造发布记录、同步至书城平台。API层根据Service层返回结果构造响应：成功返回200及发布记录，失败则返回相应错误响应。该接口支持单独发布章节，作者可完成一章发布一章，无需等待整个作品完成。具体代码如图45所示。

---

## 5.2.5 数据统计界面

### 图索引 5.2.14 作品统计API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/stats_api.go`
**函数名**：`GetBookStats`（第38-58行）
**对应章节**：5.2.5 数据统计界面
**论文图号**：图 46

**代码说明**：作品统计API层实现了获取作品统计数据的接口。函数从URL路径参数中提取作品ID，验证非空后调用`statsService.CalculateBookStats`方法计算作品统计数据。Service层聚合阅读量、收藏量、评论数、评分等数据，计算完读率、跳出率等指标，返回完整的作品统计对象。API层根据Service层返回结果构造响应：作品不存在返回404，成功则返回200及统计数据。前端据此渲染数据统计页面，以图表形式展示作品的各项指标，帮助作者了解作品表现。具体代码如图46所示。

---

### 图索引 5.2.15 章节统计API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/stats_api.go`
**函数名**：`GetChapterStats`（第71-91行）
**对应章节**：5.2.5 数据统计界面
**论文图号**：图 47

**代码说明**：章节统计API层实现了获取单个章节统计数据的接口。函数从URL路径参数中提取章节ID，验证非空后调用`statsService.CalculateChapterStats`方法计算章节统计数据。Service层返回包含阅读量、完读率、跳出率、平均阅读时长等指标的章节统计对象。API层根据Service层返回结果构造响应：章节不存在返回404，成功则返回200及统计数据。前端据此渲染章节统计面板，作者可查看每章的阅读表现，识别受欢迎或存在问题的章节。具体代码如图47所示。

---

### 图索引 5.2.16 阅读热力图API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/stats_api.go`
**函数名**：`GetBookHeatmap`（第104-119行）
**对应章节**：5.2.5 数据统计界面
**论文图号**：图 49

**代码说明**：阅读热力图API层实现了获取作品阅读热力图的接口。函数从URL路径参数中提取作品ID，验证非空后调用`statsService.GenerateHeatmap`方法生成热力图数据。Service层统计各章节的阅读量、完读率，返回按章节索引排列的热力图数据，用于可视化展示作品的阅读热度分布。API层通过`response.Success`方法返回热力图数据，前端以热力图形式渲染，颜色深浅表示阅读量高低，帮助作者快速识别作品的"黄金章节"与"冷门章节"。具体代码如图49所示。

---

## 5.2.6 稿费收入界面

### 图索引 5.2.17 收入统计API层代码

**文件位置**：`Qingyu_backend/api/v1/writer/stats_api.go`
**函数名**：`GetBookRevenue`（第134-177行）
**对应章节**：5.2.6 稿费收入界面
**论文图号**：图 50

**代码说明**：收入统计API层实现了获取作品收入统计的接口。函数从URL路径参数中提取作品ID，从查询参数获取开始日期与结束日期。若未提供日期参数，默认查询最近30天收入。解析日期字符串为时间对象，验证日期格式正确性。调用`statsService.GetRevenueBreakdown`方法获取收入细分数据，Service层按订阅收入、打赏收入、广告收入等类别统计，返回包含总收入、各分类收入、日收入趋势的收入细分对象。API层根据Service层返回结果构造响应，成功则返回200及收入统计数据。前端据此渲染稿费收入页面，以折线图、饼图等形式展示收入趋势与构成。具体代码如图50所示。

---

## 5.2.3 AI辅助创作界面

### 图索引 5.2.18 AI辅助创作代码

**文件位置**：`Qingyu_backend/api/v1/ai/writing_api.go`
**函数名**：`ContinueWriting`（第47-80行）
**对应章节**：5.2.3 AI辅助创作界面
**论文图号**：图 48

**代码说明**：AI辅助创作API层实现了智能续写功能的调用入口。函数首先通过`c.ShouldBindJSON`绑定请求体到`ContinueWritingRequest`结构体，包含`ProjectID`项目ID、`ChapterID`章节ID、`CurrentText`当前正文、`ContinueLength`续写长度、`Options`生成选项等字段。将请求转换为Service层请求格式后，生成唯一的`requestID`并注入gin.Context用于日志追踪。随后调用`aiService.ContinueWriting`方法执行AI生成逻辑：Service层调用AI服务接口，传递当前文本与续写参数，获取AI生成的续写内容。生成结果包含`GeneratedText`续写文本、`TokensUsed`消耗Token数、`Model`使用的模型名称等字段。API层将Token使用量和模型名称注入Context，最后通过`response.SuccessWithMessage`返回续写结果，前端将其回填编辑区实现"生成-调整-保存"闭环。具体代码如图48所示。

---

## 5.3 管理员端功能模块

### 图索引 5.3.1 用户列表查询代码

**文件位置**：`Qingyu_backend/api/v1/admin/user_admin_api.go`
**函数名**：`ListUsers`（第43-70行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 56

**代码说明**：用户列表查询API层实现了管理员获取用户列表的功能。函数从URL查询参数中解析`page`页码、`size`每页数量、`keyword`搜索关键词、`role`角色筛选、`status`状态筛选等参数，构造`UserFilter`过滤器对象。调用`userAdminService.GetUserList`方法执行业务逻辑：Service层根据筛选条件构造数据库查询，支持用户名、邮箱的模糊搜索及角色、状态的精确匹配。查询结果包含用户数组与总数，API层通过`gin.H`构造分页响应对象后调用`response.Success`返回前端。具体代码如图56所示。

---

### 图索引 5.3.2 创建用户代码

**文件位置**：`Qingyu_backend/api/v1/admin/user_admin_api.go`
**函数名**：`CreateUser`（第535-567行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 57

**代码说明**：创建用户API层实现了管理员手动创建用户的功能。函数通过`c.ShouldBindJSON`绑定请求体到`CreateUserRequest`结构体，包含`Username`用户名、`Email`邮箱、`Password`密码（可选，为空则自动生成）、`Nickname`昵称、`Role`角色、`Status`状态、`Bio`简介等字段。通过结构体标签进行参数校验：用户名长度3-50字符，邮箱格式验证，角色必须是reader/author/admin之一。将API请求转换为Service请求格式后调用`userAdminService.CreateUser`方法执行创建逻辑：Service层检查邮箱唯一性，若已存在则返回`ErrUserAlreadyExists`错误；密码为空时通过生成随机密码。创建成功后返回用户完整信息。具体代码如图57所示。

---

### 图索引 5.3.3 批量创建用户代码

**文件位置**：`Qingyu_backend/api/v1/admin/user_admin_api.go`
**函数名**：`BatchCreateUsers`（第582-612行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 58

**代码说明**：批量创建用户API层支持管理员一次性创建多个测试用户。函数通过`c.ShouldBindJSON`绑定请求体到`BatchCreateUserRequest`结构体，包含`Count`创建数量（1-100）、`Prefix`用户名前缀、`Role`默认角色、`Status`初始状态等字段。将API请求转换为Service请求格式后调用`userAdminService.BatchCreateUsers`方法执行批量创建逻辑：Service层循环调用创建方法，为每个用户生成唯一的用户名（前缀+序号）与随机密码，批量插入数据库后返回用户列表。该功能用于系统测试与演示环境的快速初始化。具体代码如图58所示。

---

### 图索引 5.3.4 删除用户代码

**文件位置**：`Qingyu_backend/api/v1/admin/user_admin_api.go`
**函数名**：`DeleteUser`（第213-234行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 59

**代码说明**：删除用户API层实现了管理员删除用户的功能。函数从URL路径参数中提取用户ID，若为空则返回400参数错误。调用`userAdminService.DeleteUser`方法执行删除逻辑：Service层首先检查用户是否存在，不存在则返回`ErrUserNotFound`错误；随后检查是否为超级管理员，超级管理员不允许删除，返回`ErrCannotModifySuperAdmin`错误。删除操作采用软删除方式，将用户状态设为`deleted`而非物理删除记录，保留历史数据便于审计。API层根据Service层返回的错误类型构造响应：用户不存在返回404，权限不足返回403，成功则返回200。具体代码如图59所示。

---

### 图索引 5.3.5 用户管理API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/user_admin_api.go`
**函数名**：`CreateUser`（第535-567行） / `DeleteUser`（第213-234行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 60

**代码说明**：用户管理API层体现了管理员用户操作的接口处理逻辑。创建用户时，API层绑定请求参数并验证合法性，调用Service层的`CreateUser`方法执行邮箱唯一性校验、密码哈希加密、用户记录创建等业务逻辑。删除用户时，API层提取用户ID参数，调用Service层的`DeleteUser`方法，Service层执行权限检查（保护超级管理员不被删除）与软删除操作。API层根据Service层返回结果构造响应，将业务错误转换为对应的HTTP状态码返回前端。API层不直接访问数据库，所有业务规则由Service层统一管理，确保多入口操作时逻辑一致。具体代码如图60所示。

---

### 图索引 5.3.6 封禁用户代码

**文件位置**：`Qingyu_backend/api/v1/admin/user_admin_api.go`
**函数名**：`UpdateUserStatus`（第121-180行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 61

**代码说明**：封禁用户API层实现了将用户状态设置为banned的接口。函数从URL路径参数中提取用户ID，通过`c.ShouldBindJSON`绑定请求体到`UpdateUserStatusRequest`结构体。验证逻辑：当`status`字段设为"banned"时，`banReason`字段必填，否则返回400错误。获取当前操作者ID后调用Service层的`UpdateUserStatusWithReason`方法执行封禁逻辑：Service层更新用户状态为banned，设置封禁时间、操作者ID和封禁原因，同时记录封禁历史到BanRecord集合。解封时将状态设为"active"即可，系统自动清除封禁字段。API层根据Service层返回结果构造响应，成功返回200状态码。具体代码如图61所示。

---

### 图索引 5.3.7 用户模型封禁字段代码

**文件位置**：`Qingyu_backend/models/users/user.go`
**结构体**：`User`（第23-52行）
**对应章节**：5.3.1 用户管理界面
**论文图号**：图 62

**代码说明**：用户模型中新增了三个封禁相关字段：`BanReason`封禁原因、`BannedAt`封禁时间（指针类型，未封禁时为nil）、`BannedBy`封禁操作者ID。这三个字段设为可选（omitempty标签），仅在用户被封禁时有值。模型同时提供了`IsBanned()`方法，通过检查`Status == UserStatusBanned`快速判断用户是否处于封禁状态。解封时，Service层将这些字段清空即可恢复用户为活跃状态。封禁记录同时保存在独立的BanRecord集合中，用于审计追踪和历史查询。具体代码如图62所示。

---

## 5.3.2 内容审核界面

### 图索引 5.3.8 获取待审核内容列表API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/audit_admin_api.go`
**函数名**：`GetPendingAudits`（第40-70行）
**对应章节**：5.3.2 内容审核界面
**论文图号**：图 63

**代码说明**：获取待审核内容列表API层实现了管理员查看待审核内容的接口。函数从URL查询参数中解析`targetType`目标类型（document/chapter/comment）、`page`页码、`limit`每页数量等参数。调用`auditService.GetPendingReviews`方法获取待审核记录列表。Service层返回包含审核ID、目标类型、目标内容、提交时间、风险等级等信息的审核记录数组。API层通过`response.Success`方法返回待审核列表，前端据此渲染内容审核页面，管理员可查看每条待审核内容的具体信息并进行审核操作。该接口支持按目标类型过滤，帮助管理员快速定位特定类型的内容审核任务。具体代码如图63所示。

---

### 图索引 5.3.9 审核通过/拒绝API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/audit_admin_api.go`
**函数名**：`ReviewAudit`（第88-124行）
**对应章节**：5.3.2 内容审核界面
**论文图号**：图 64

**代码说明**：审核API层实现了管理员审核内容的接口。函数从URL路径参数中提取审核记录ID，通过`c.ShouldBindJSON`绑定请求体到`ReviewAuditRequest`结构体，包含`Action`审核动作（approve/reject）、`ReviewNote`审核意见等字段。从gin.Context中获取当前登录管理员的ID用于审核记录。根据`Action`字段判断审核结果：`approve`表示通过，其他值表示拒绝。调用`auditService.ReviewAudit`方法执行审核逻辑：Service层更新审核记录状态、记录审核人ID和审核意见、通知提交者审核结果。API层根据Service层返回结果构造响应，成功则返回200及相应提示信息。该接口是内容审核的核心功能，确保平台内容的质量合规性。具体代码如图64所示。

---

### 图索引 5.3.10 批量审核API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/audit_admin_api.go`
**函数名**：`BatchReviewAudit`（第260-292行）
**对应章节**：5.3.2 内容审核界面
**论文图号**：图 65

**代码说明**：批量审核API层实现了管理员一次性审核多个内容的接口。函数通过`c.ShouldBindJSON`绑定请求体到`BatchReviewAuditRequest`结构体，包含`AuditIDs`审核记录ID数组、`Action`统一审核动作、`ReviewNote`统一审核意见等字段。从gin.Context中获取当前管理员ID后，初始化批量操作结果对象，包含总数、成功数、失败数及错误信息。遍历审核记录ID数组，调用`auditService.ReviewAudit`方法逐个执行审核操作，统计成功与失败数量。API层返回批量操作结果，包含总处理数、成功数、失败数及详细错误信息，方便管理员了解批量审核的执行情况。该接口提高了审核效率，适用于处理大量相似类型的待审核内容。具体代码如图65所示。

---

### 图索引 5.3.11 获取高风险审核记录API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/audit_admin_api.go`
**函数名**：`GetHighRiskAudits`（第195-219行）
**对应章节**：5.3.2 内容审核界面
**论文图号**：图 66

**代码说明**：获取高风险审核记录API层实现了管理员查看高风险内容的接口。函数从URL查询参数中解析`minRiskLevel`最低风险等级（默认3）、`limit`数量限制（默认50）等参数。调用`auditService.GetHighRiskAudits`方法获取高风险审核记录列表，Service层根据风险评分筛选出高于指定阈值的内容，返回包含审核ID、风险等级、风险因素、目标内容等信息的记录数组。高风险内容通常包含敏感词汇、违规内容或用户举报较多的项目，需要管理员优先处理。API层通过`response.Success`方法返回高风险记录列表，前端可突出显示或置顶这些高风险项，提醒管理员及时处理。该接口帮助管理员识别重要审核任务，提升平台内容安全管控能力。具体代码如图66所示。

---

### 图索引 5.3.12 获取审核统计API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/audit_admin_api.go`
**函数名**：`GetAuditStatistics`（第234-243行）
**对应章节**：5.3.2 内容审核界面
**论文图号**：图 67

**代码说明**：获取审核统计API层实现了查看审核统计数据的接口。函数调用`auditService.GetAuditStatistics`方法获取审核统计信息，Service层聚合待审核数、已通过数、已拒绝数、平均审核时长、今日审核数等统计数据。API层通过`response.Success`方法返回统计结果，前端据此渲染审核仪表盘，以卡片或图表形式展示审核工作量、处理效率等关键指标。该接口为管理员提供了审核工作的宏观视图，帮助评估审核负荷与效率，支持审核资源的合理配置与调度。具体代码如图67所示。

---

## 5.3.3 分类管理界面

### 图索引 5.3.13 创建分类API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/category_admin_api.go`
**函数名**：`CreateCategory`（第34-48行）
**对应章节**：5.3.3 分类管理界面
**论文图号**：图 68

**代码说明**：创建分类API层实现了管理员创建书籍分类的接口。函数通过`c.ShouldBindJSON`绑定请求体到`CreateCategoryRequest`结构体，包含分类名称、父分类ID、描述等字段。调用`categoryService.CreateCategory`方法执行创建逻辑：Service层验证同级分类名称唯一性、检查层级深度限制（最多3层）、创建分类记录并返回完整信息。API层根据Service层返回结果构造响应：成功返回200及分类详情，失败则返回相应错误响应。该接口支持创建父子层级分类，便于组织书城的内容结构。具体代码如图68所示。

---

### 图索引 5.3.14 获取分类树API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/category_admin_api.go`
**函数名**：`GetCategoryTree`（第84-92行）
**对应章节**：5.3.3 分类管理界面
**论文图号**：图 68a

**代码说明**：获取分类树API层实现了获取完整分类层级结构的接口。函数调用`categoryService.GetCategoryTree`方法获取分类树数据，Service层从MongoDB查询所有分类记录，通过递归算法构建父子关系，按排序字段`sort_order`升序排列同级分类，返回树形结构数据。树形结构包含分类ID、名称、子分类数组、作品数量等字段，前端可据此渲染可折叠的分类树组件。该接口仅9行代码，职责单一，符合API层"薄接口"的设计原则。具体代码如图68a所示。

---

### 图索引 5.3.15 更新分类API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/category_admin_api.go`
**函数名**：`UpdateCategory`（第132-152行）
**对应章节**：5.3.3 分类管理界面
**论文图号**：图 68b

**代码说明**：更新分类API层实现了修改分类信息的接口。函数从URL路径参数中提取分类ID，验证非空后通过`c.ShouldBindJSON`绑定请求体到`UpdateCategoryRequest`结构体，包含分类名称、描述、图标、启用状态等字段。调用`categoryService.UpdateCategory`方法执行更新逻辑：Service层验证同级分类名称唯一性（排除自身）、检查层级移动后的深度是否超限、更新分类字段并保存至数据库。API层根据Service层返回结果构造响应：成功返回200及更新后的分类信息，失败则返回相应错误响应。该接口支持修改分类名称、图标、启用状态等属性。具体代码如图68b所示。

---

### 图索引 5.3.16 删除分类API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/category_admin_api.go`
**函数名**：`DeleteCategory`（第165-178行）
**对应章节**：5.3.3 分类管理界面
**论文图号**：图 68c

**代码说明**：删除分类API层实现了删除分类的接口，采用安全删除策略。函数从URL路径参数中提取分类ID，验证非空后调用`categoryService.DeleteCategory`方法执行删除逻辑。Service层首先检查分类是否存在，随后检查是否有子分类，存在子分类则不允许删除，返回错误提示需先删除子分类。接着检查该分类下是否有关联作品，存在作品则不允许删除，返回错误提示需先移除作品。通过所有安全检查后执行删除操作。API层根据Service层返回结果构造响应：成功返回200，失败则返回相应错误响应。该接口确保删除操作的安全性，防止误删导致的数据孤岛问题。具体代码如图68c所示。

---

### 图索引 5.3.17 移动分类API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/category_admin_api.go`
**函数名**：`MoveCategory`（第191-210行）
**对应章节**：5.3.3 分类管理界面
**论文图号**：图 68d

**代码说明**：移动分类API层实现了将分类移动到新父分类下的接口。函数从URL路径参数中提取分类ID，通过`c.ShouldBindJSON`绑定请求体到`MoveCategoryRequest`结构体，包含目标父分类ID字段。调用`categoryService.MoveCategory`方法执行移动逻辑：Service层验证新父分类存在性、检查移动后的层级深度是否超限（最多3层）、检测循环引用（如将父分类移动到其子分类下）、更新分类的父关系并保存。API层根据Service层返回结果构造响应：成功返回200，失败则返回相应错误响应。该接口支持调整分类的层级结构，便于管理员优化书城的分类组织。具体代码如图68d所示。

---

### 图索引 5.3.18 调整排序API层代码

**文件位置**：`Qingyu_backend/api/v1/admin/category_admin_api.go`
**函数名**：`SortCategory`（第223-244行）
**对应章节**：5.3.3 分类管理界面
**论文图号**：图 68e

**代码说明**：调整排序API层实现了修改分类显示顺序的接口。函数从URL路径参数中提取分类ID，通过`c.ShouldBindJSON`绑定请求体到排序请求结构体，包含`sort_order`排序序号字段。调用`categoryService.SortCategory`方法执行排序调整逻辑：Service层更新分类的`sort_order`字段，保存至数据库。前端可通过拖拽方式调整分类顺序，API接收新的序号并持久化，下次获取分类树时按序号升序排列显示。API层根据Service层返回结果构造响应：成功返回200，失败则返回相应错误响应。该接口为管理员提供了灵活的分类排序能力，提升用户体验。具体代码如图68e所示。

---

## 5.4 AI服务模块

### 图索引 5.4.1 AI健康检查代码

**文件位置**：`Qingyu_backend/api/v1/ai/system_api.go`
**函数名**：`HealthCheck`（第32-40行）
**对应章节**：5.4.1 AI网关与模型路由
**论文图号**：图 69

**代码说明**：AI健康检查API层实现了AI服务状态监控的接口。函数构造包含服务状态、时间戳、服务名称的响应对象，通过`response.Success`方法返回。该接口用于系统健康监控，前端或监控系统可通过调用此接口检查AI服务的可用性。API层不调用任何Service层方法，直接返回静态状态信息，是API层中最简单的实现示例，体现了"单一职责"原则。具体代码如图69所示。

---

### 图索引 5.4.2 RAG检索增强服务代码

**文件位置**：`Qingyu_backend/api/v1/ai/rag_api.go`
**函数名**：`RetrieveAndGenerate`（第65-105行）
**对应章节**：5.4.2 RAG检索增强服务
**论文图号**：图 71

**代码说明**：RAG检索API层实现了"检索前处理 -> 召回 -> 重排 -> 生成"四段式流程的接口入口。函数通过`c.ShouldBindJSON`绑定请求体到`RetrieveAndGenerateRequest`结构体，包含`Query`查询内容、`TopK`返回数量、`ProjectID`项目ID、`IncludeOutline`是否包含大纲等字段。验证用户身份后调用`ragService.RetrieveAndGenerate`方法执行检索增强生成逻辑：Service层通过嵌入服务将问题转换为向量表示，执行向量检索获取语义相似的Top-K个文本片段，对召回结果进行相关性重排序，筛选出最相关的片段，最后将检索片段拼接为上下文，通过LLM生成答案并返回引用片段。API层将生成结果封装为统一响应格式返回前端，包含答案内容、引用来源、Token消耗量等元数据，提升结果的可解释性与准确性。具体代码如图71所示。

---

## 附录使用说明

### 1. 截图方式建议
使用代码编辑器（如VSCode）打开对应文件，定位到指定函数或行号，选择关键逻辑部分（建议15-35行），确保：
- 代码字体大小适中（建议14-16px）
- 行号清晰可见
- 代码高亮配色对比度足够
- 截图宽度适合论文排版（建议1200-1400px宽）

### 2. 图题格式
论文中应按"图 X 代码功能描述"格式添加图题，例如：
```
图 2 用户登录验证代码
```
图题置于截图下方居中位置，使用小五号或五号字体。

### 3. 代码说明使用
上述代码说明可直接复制粘贴至论文正文，作为对应截图的文字描述。所有说明均描述API层的职责：
- 参数绑定与验证
- 调用Service层方法
- 构造响应返回

### 4. 完整源码引用
论文正文中可添加"完整源码见附录A"或"完整源码见代码仓库"说明，指向项目代码库或附录文档。

---

## 文件路径索引速查表

| 章节    | 功能      | 文件路径                                     | 函数名                             |
| ----- | ------- | ---------------------------------------- | ------------------------------- |
| 5.1.1 | 用户登录    | `api/v1/auth/auth_api.go`                | `Login` (L77-91)                |
| 5.1.1 | 用户注册    | `api/v1/auth/auth_api.go`                | `Register` (L38-62)             |
| 5.1.2 | 首页书籍    | `api/v1/bookstore/bookstore_api.go`      | `GetHomepage` (L64-72)          |
| 5.1.3 | 书城筛选    | `api/v1/bookstore/bookstore_api.go`      | `SearchBooks` (L314-553)        |
| 5.1.4 | 书籍详情    | `api/v1/bookstore/book_detail_api.go`    | `GetBookDetail` (L37-55)        |
| 5.1.5 | 章节阅读    | `api/v1/reader/chapter_api.go`           | `GetChapterContent` (L52-85)    |
| 5.1.5 | 阅读进度    | `api/v1/reader/progress_api.go`          | `SaveReadingProgress` (L79-98)  |
| 5.1.6 | 评论发表    | `api/v1/social/comment_api.go`           | `CreateComment` (L48-76)        |
| 5.1.7 | 添加收藏    | `api/v1/social/collection_api.go`        | `AddCollection` (L47-83)        |
| 5.1.7 | 收藏列表    | `api/v1/social/collection_api.go`        | `GetCollections` (L209-233)     |
| 5.1.8 | 个人信息    | `api/v1/user/handler/profile_handler.go` | `GetProfile` (L48-78)           |
| 5.2.1 | 创建作品    | `api/v1/writer/project_api.go`           | `CreateProject` (L37-57)        |
| 5.2.1 | 作品列表    | `api/v1/writer/project_api.go`           | `ListProjects` (L99-125)        |
| 5.2.1 | 作品详情    | `api/v1/writer/project_api.go`           | `GetProject` (L69-85)           |
| 5.2.1 | 更新作品    | `api/v1/writer/project_api.go`           | `UpdateProject` (L139-160)      |
| 5.2.1 | 删除作品    | `api/v1/writer/project_api.go`           | `DeleteProject` (L172-187)      |
| 5.2.2 | 创建章节    | `api/v1/writer/document_api.go`          | `CreateDocument` (L47-93)       |
| 5.2.2 | 更新章节    | `api/v1/writer/document_api.go`          | `UpdateDocument` (L151-168)     |
| 5.2.2 | 自动保存    | `api/v1/writer/editor_api.go`            | `AutoSaveDocument` (L40-69)     |
| 5.2.2 | 获取章节内容  | `api/v1/writer/editor_api.go`            | `GetDocumentContent` (L107-123) |
| 5.2.3 | AI创作    | `api/v1/ai/writing_api.go`               | `ContinueWriting` (L47-80)      |
| 5.2.4 | 发布作品    | `api/v1/writer/publish_api.go`           | `PublishProject` (L36-65)       |
| 5.2.4 | 取消发布    | `api/v1/writer/publish_api.go`           | `UnpublishProject` (L78-101)    |
| 5.2.4 | 发布状态查询  | `api/v1/writer/publish_api.go`           | `GetProjectPublicationStatus` (L113-128) |
| 5.2.4 | 发布章节    | `api/v1/writer/publish_api.go`           | `PublishDocument` (L143-179)    |
| 5.2.5 | 作品统计    | `api/v1/writer/stats_api.go`             | `GetBookStats` (L38-58)         |
| 5.2.5 | 章节统计    | `api/v1/writer/stats_api.go`             | `GetChapterStats` (L71-91)      |
| 5.2.5 | 阅读热力图   | `api/v1/writer/stats_api.go`             | `GetBookHeatmap` (L104-119)     |
| 5.2.6 | 收入统计    | `api/v1/writer/stats_api.go`             | `GetBookRevenue` (L134-177)     |
| 5.3.1 | 用户列表    | `api/v1/admin/user_admin_api.go`         | `ListUsers` (L43-70)            |
| 5.3.1 | 创建用户    | `api/v1/admin/user_admin_api.go`         | `CreateUser` (L535-567)         |
| 5.3.1 | 批量创建    | `api/v1/admin/user_admin_api.go`         | `BatchCreateUsers` (L582-612)   |
| 5.3.1 | 删除用户    | `api/v1/admin/user_admin_api.go`         | `DeleteUser` (L213-234)         |
| 5.3.1 | 封禁用户    | `api/v1/admin/user_admin_api.go`         | `UpdateUserStatus` (L121-180)   |
| 5.3.1 | 用户模型    | `models/users/user.go`                   | `User` struct (L23-52)          |
| 5.3.2 | 待审核列表    | `api/v1/admin/audit_admin_api.go`        | `GetPendingAudits` (L40-70)     |
| 5.3.2 | 审核通过/拒绝  | `api/v1/admin/audit_admin_api.go`        | `ReviewAudit` (L88-124)         |
| 5.3.2 | 批量审核    | `api/v1/admin/audit_admin_api.go`        | `BatchReviewAudit` (L260-292)   |
| 5.3.2 | 高风险记录    | `api/v1/admin/audit_admin_api.go`        | `GetHighRiskAudits` (L195-219)   |
| 5.3.2 | 审核统计    | `api/v1/admin/audit_admin_api.go`        | `GetAuditStatistics` (L234-243)  |
| 5.3.3 | 创建分类    | `api/v1/admin/category_admin_api.go`    | `CreateCategory` (L34-48)        |
| 5.3.3 | 获取分类树   | `api/v1/admin/category_admin_api.go`    | `GetCategoryTree` (L84-92)       |
| 5.3.3 | 更新分类    | `api/v1/admin/category_admin_api.go`    | `UpdateCategory` (L132-152)      |
| 5.3.3 | 删除分类    | `api/v1/admin/category_admin_api.go`    | `DeleteCategory` (L165-178)      |
| 5.3.3 | 移动分类    | `api/v1/admin/category_admin_api.go`    | `MoveCategory` (L191-210)        |
| 5.3.3 | 调整排序    | `api/v1/admin/category_admin_api.go`    | `SortCategory` (L223-244)        |
| 5.4.1 | AI健康检查  | `api/v1/ai/system_api.go`                | `HealthCheck` (L32-40)          |
| 5.4.2 | RAG检索增强 | `api/v1/ai/rag_api.go`                   | `RetrieveAndGenerate` (L65-105) |
| 5.4.2 | 相似内容搜索  | `api/v1/ai/rag_api.go`                   | `SearchSimilar` (L117-152)      |

**注**：
1. 所有函数已确认无技术债问题，均为单一职责的清晰实现
2. 2026-02-26完成TDD驱动重构，API层职责更清晰，代码质量显著提升
3. 所有行号已验证与实际代码一致，函数名已更新为实际使用的名称
4. 代码说明已更新，反映API层简化后的实现方式

**API层简化前后对比（2026-02-27验证）**：
- `SearchByTitle`: 从~150行简化到L569-603（35行）
- `SearchByAuthor`: 从~150行简化到L633-667（35行）
- `GetSimilarBooks`: 从~65行简化到L1182-1215（34行）

---

## API层代码特点说明

本附录中所有截图均为API层代码，具有以下共同特点：

1. **职责单一**：仅负责请求参数处理、Service层调用、响应封装
2. **代码简洁**：通常15-40行，适合单图展示
3. **依赖注入**：通过构造函数注入Service层依赖
4. **错误处理**：将Service层业务错误转换为HTTP状态码
5. **响应统一**：使用`response.Success`/`response.BadRequest`等方法封装响应

这种设计确保API层代码清晰易懂，适合在论文中展示系统的接口实现方式。

### 图索引 5.4.3 相似内容搜索代码

**文件位置**：`Qingyu_backend/api/v1/ai/rag_api.go`
**函数名**：`SearchSimilar`（第117-152行）
**对应章节**：5.4.2 RAG检索增强服务
**论文图号**：图 72

**代码说明**：相似内容搜索API层实现了基于向量语义相似度的文档片段检索接口。函数从URL查询参数中提取`query`查询内容，验证查询内容非空。解析`topK`返回数量（默认5，最大100），验证用户身份后调用`ragService.SearchSimilar`方法执行向量相似度搜索：Service层将查询内容转换为向量表示，在向量数据库中执行相似度检索，返回最相关的Top-K个文档片段。API层将结果直接返回前端，用于文档引用、相关推荐等场景。该接口为RAG检索的核心组件，为知识检索增强提供基础能力。具体代码如图72所示。

---

## 更新记录

- **2026-03-01**: 补充管理员分类管理功能模块
  - 新增5.3.3分类管理界面章节
  - 新增6个分类管理API代码条目：CreateCategory、GetCategoryTree、UpdateCategory、DeleteCategory、MoveCategory、SortCategory
  - 新增图号：图68、图68a、图68b、图68c、图68d、图68e
  - 更新速查表，添加6条分类管理相关条目
  - 代码文件位置：`api/v1/admin/category_admin_api.go`

- **2026-02-27**: 全面验证并更新所有函数名和行号，确保与实际代码一致
  - 修正函数名：`GetHomepageBooks` → `GetHomepage`，`GetChapter` → `GetChapterContent`，`AddToCollection` → `AddCollection`，`ListCollections` → `GetCollections`
  - 更新行号：所有函数的实际行号已验证
  - 更新代码说明：反映API层简化后的实现方式
  - 新增速查表条目：`RetrieveAndGenerate`（L65-105），`SearchSimilar`（L117-152）
  - 移除不存在的函数：`HandleAIRequest`替换为`HealthCheck`

- **2026-02-26**: 完成TDD驱动重构，API层职责更清晰，代码质量显著提升

---

## 序列图附录

本附录提供系统核心业务流程的序列图，展示各组件之间的交互过程。所有序列图均采用学术黑白风格设计，适合论文插图使用。

### 图索引 5.5.1 读者端书城浏览序列图

**文件位置**：`论文/draw/图5-28-读者端书城浏览序列图_academic.drawio`
**对应章节**：5.1 读者端功能模块 - 5.1.3 书城浏览界面

**流程说明**：书城浏览序列图展示了读者从进入书城到筛选作品的完整交互过程。读者打开书城页面后，前端向API网关发起GET请求获取作品列表。作品服务优先查询Redis缓存中是否存在热门作品列表数据，缓存命中则直接返回，减少数据库查询压力。当读者选择特定分类筛选时，缓存未命中触发MongoDB数据库查询，获取该分类下的作品数据，并同步更新Redis缓存以便后续请求复用。整个流程体现了"缓存优先"的设计策略，通过减少数据库查询提升系统响应速度。序列图中参与者包括读者、前端应用、API网关、作品服务、缓存服务（Redis）和内容数据库（MongoDB），展示了六层架构间的消息传递顺序。具体如图5.5.1所示。

**技术要点**：
- 缓存策略：Redis缓存热门作品列表，采用TTL过期策略确保数据新鲜度
- 分页加载：支持分页查询，前端滚动触发自动加载更多
- 多维筛选：支持按分类、标签、状态、作者等条件组合筛选
- 灵活排序：按热度、更新时间、收藏数等多维度排序

---

### 图索引 5.5.2 作者端章节创作序列图

**文件位置**：`论文/draw/图5-29-作者端章节创作序列图_academic.drawio`
**对应章节**：5.2 作者端功能模块 - 5.2.2 章节创作界面

**流程说明**：章节创作序列图展示了作者从创建章节草稿到使用AI辅助续写的完整工作流。作者点击新建章节后，前端调用章节服务的创建接口，Service层在MongoDB中插入章节草稿记录并返回章节ID，前端打开富文本编辑器供作者编辑。当作者使用AI续写功能时，后端通过gRPC协议调用AI服务，AI服务首先进行RAG检索，从向量数据库中获取相关章节的语义上下文，确保生成内容与作品风格一致。随后AI服务调用大语言模型，结合检索到的上下文生成续写内容并返回。作者可在编辑器中查看和修改AI生成内容，确认后保存章节。序列图中的虚线框标识了AI服务边界，内部包含向量检索和大模型调用两个关键步骤。具体如图5.5.2所示。

**技术要点**：
- 自动保存：编辑器定时自动保存草稿，防止内容丢失
- 版本控制：章节支持多版本管理，可回溯历史版本
- AI辅助：RAG检索提供上下文，保持内容连贯性与风格统一
- 协议通信：后端与AI服务采用gRPC高效通信，支持流式响应

---

### 图索引 5.5.3 AI功能调用序列图（已存在）

**文件位置**：`论文/draw/图5-xx-AI功能使用序列图_academic.drawio`
**对应章节**：5.4 AI服务模块

**流程说明**：该序列图展示了AI功能调用的完整流程，包括用户发起请求、前端转发、后端调用AI服务、AI服务进行RAG检索和大模型调用的全过程。流程与作者端章节创作序列图中的AI部分类似，但更为通用，适用于所有AI辅助功能场景。具体如图5.xx所示。

---

### 图索引 5.5.4 JWT认证序列图（已存在）

**文件位置**：`论文/draw/图5-xx-JWT序列图.drawio`
**对应章节**：5.1.1 登录注册界面

**流程说明**：该序列图展示了用户登录认证和请求验证两个阶段。登录阶段：用户提交用户名密码，后端验证凭据，认证服务签发JWT Token，前端存储Token。请求验证阶段：前端请求携带Token，网关提取并验证Token有效性，验证通过后执行业务逻辑并返回数据。具体如图5.xx所示。

---

### 序列图使用说明

**1. 文件格式**
- 原始文件：`.drawio`格式，使用draw.io软件打开编辑
- 导出格式：建议导出为PNG或SVG格式用于论文插图
- 分辨率：导出时设置DPI为300，确保打印清晰度

**2. 论文引用**
序列图应在论文正文中按以下格式引用：
```
如图5.5.1所示，读者端书城浏览流程...
如图5.5.2所示，作者端章节创作流程...
```

**3. 图表风格**
所有序列图采用统一的学术黑白风格：
- 背景色：纯白色（#FFFFFF）
- 边框色：黑色（#000000）
- 生命线：黑色虚线
- 消息线：实线箭头表示请求，虚线箭头表示响应
- 字体：宋体（SimSun），大小10-12pt

---

### 序列图速查表

| 图号   | 序列图名称       | 对应章节   | 核心参与者数量 | 关键技术               |
| ----- | ---------- | ----- | ------ | -------------------- |
| 5.5.1 | 读者端书城浏览   | 5.1.3 | 6      | Redis缓存、分页查询       |
| 5.5.2 | 作者端章节创作   | 5.2.2 | 6      | gRPC、RAG检索、自动保存   |
| 5.xx  | AI功能调用    | 5.4   | 6      | 向量检索、大模型调用        |
| 5.xx  | JWT认证流程   | 5.1.1 | 6      | Token签发、权限验证        |

**注**：所有序列图遵循UML序列图规范，垂直方向表示时间轴，水平方向表示不同组件间的消息传递。

---

*本附录由项目代码分析生成，所有代码位置准确对应实际API层实现。最后更新：2026-03-01*

## 更新记录

- **2026-03-01**: 重大更新 - 删除重复内容并补充缺失章节
  - 删除9个重复的代码条目（读者端4个+作者端5个）
  - 补充5.2.2章节编写界面：CreateDocument、UpdateDocument、AutoSaveDocument、GetDocumentContent
  - 补充5.2.4发布管理界面：PublishProject、UnpublishProject、GetProjectPublicationStatus、PublishDocument
  - 补充5.2.5数据统计界面：GetBookStats、GetChapterStats、GetBookHeatmap
  - 补充5.2.6稿费收入界面：GetBookRevenue
  - 补充5.3.2内容审核界面：GetPendingAudits、ReviewAudit、BatchReviewAudit、GetHighRiskAudits、GetAuditStatistics
  - 更新速查表，新增18个代码条目
  - 调整章节图号，确保与论文结构一致

- **2026-02-27**: 全面验证并更新所有函数名和行号，确保与实际代码一致
  - 修正函数名：`GetHomepage` → `GetHomepage`，`GetChapter` → `GetChapterContent`，`AddToCollection` → `AddCollection`，`ListCollections` → `GetCollections`
  - 更新行号：所有函数的实际行号已验证
  - 更新代码说明：反映API层简化后的实现方式
  - 新增速查表条目：`RetrieveAndGenerate`（L65-105），`SearchSimilar`（L117-152）
  - 移除不存在的函数：`HandleAIRequest`替换为`HealthCheck`

- **2026-02-26**: 完成TDD驱动重构，API层职责更清晰，代码质量显著提升
